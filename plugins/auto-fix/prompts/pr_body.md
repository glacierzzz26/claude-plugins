Automated fix for #{issue}, produced by the **auto-fix** plugin.

> **This PR must be merged by a human.** No automated step merges it, and it is
> opened as a draft on purpose. Review the diff before marking it ready.

## Review summary

{summary}

## Run details

| | |
|---|---|
| Issue | #{issue} |
| Rounds used | {rounds} |
| Branch | `{branch}` |
| Reviewed commit | `{reviewed_sha}` |

The reviewer is a **different model** from the one that wrote the change, and it
reviewed the exact commit above. Every round was committed, so the review is
anchored to a real git object rather than to a working tree.

## Before merging

- [ ] Read the diff — the models can be confidently wrong.
- [ ] Confirm the issue is actually resolved, not just made to pass tests.
- [ ] Check nothing outside the issue's scope was changed.
