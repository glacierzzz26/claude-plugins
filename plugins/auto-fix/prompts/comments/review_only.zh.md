**auto-fix 开了草稿 PR,但验证仅为审查级别。**

本仓库未配置测试命令(或没有 `.claude/autofix.toml`),因此该改动**无法**通过运行
任何东西来验证。第二个模型审查并批准了 diff,但自动审查不能替代测试套件 —— 所以
这次运行被标记为需人工关注,而不是报告为一次成功的修复。

草稿 PR:{pr_url}

在 `.claude/autofix.toml` 中加上 `commands.test` 条目,即可让之后的运行具备完整验证。
