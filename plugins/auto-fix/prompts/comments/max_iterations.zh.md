**auto-fix 在 {rounds} 轮后仍未收敛,已停止。需要人工接手。**

fixer 和 reviewer 来回协商了最多的 {rounds} 轮仍未能达成一致,因此**未开 PR**。
目前的工作已推送到分支 `{branch}`。

### reviewer 仍未认可的问题

{unresolved}

### 下一步

自动循环不会自行解决这个问题 —— 再多几轮也只是重复同样的分歧。可选:

1. 检出 `{branch}` 手动完成改动,然后自己开 PR。
2. 修改 issue,澄清"修复"的标准,然后移除并重新打上 `auto-fix` 标签再试一次。
3. 如果这个分歧反映了需求本身确实存在歧义,关闭该 issue。
