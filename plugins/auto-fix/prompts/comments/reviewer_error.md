**auto-fix could not complete: the reviewer phase failed.**

{detail}

No PR was opened. The change may be fine, but it was never reviewed, so the
plugin refuses to open a PR. Check the run log and verify the reviewer model
profile.
