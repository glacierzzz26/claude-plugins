**auto-fix did not start: the label was applied by an account without write
access.**

{detail}

The plugin runs two paid model calls per round, so it only accepts work
requested by someone with write access to this repository. A maintainer can
apply the `auto-fix` label directly to run it.
