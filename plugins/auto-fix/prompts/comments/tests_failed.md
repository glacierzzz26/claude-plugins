**auto-fix could not get the project's tests to pass.**

The fixer's last attempt still failed the configured test command, so no PR was
opened.

```
{detail}
```

Check whether the failure is pre-existing on the base branch — if it is,
auto-fix is fighting something unrelated to this issue.
