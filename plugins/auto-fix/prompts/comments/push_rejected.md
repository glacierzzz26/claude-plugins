**auto-fix could not push its branch.**

{detail}

This normally means someone else changed the branch while the run was in
progress — the plugin refuses to overwrite a human's commits. No PR was opened.
Check the branch and the run log.
