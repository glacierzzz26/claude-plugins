**auto-fix 未做任何改动。** fixer 跑完后没有修改任何文件,因此没有可审查的内容,
也未开 PR。

这通常意味着以下之一:

- 该 issue 在 base 分支上已经修复;
- 该 issue 按现有描述无法执行(缺少复现步骤、范围不清);
- fixer 没能定位到相关代码。

如果该 issue 仍然有效,请补充细节(一个失败的测试、文件路径、预期行为),
然后重新打上 `auto-fix` 标签。
