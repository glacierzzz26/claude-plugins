**auto-fix stopped: the review findings are repeating.**

The reviewer raised the same set of issues two rounds in a row, so the fixer is
not making progress — it is trading one problem for another. Rather than burn
the remaining rounds, the loop stopped early.

The work so far is on the branch `{branch}` (pushed). No PR was opened.

The disagreement usually means the issue is ambiguous, or the fixer's approach
is wrong in a way it cannot see. A human should either finish it by hand or
clarify the issue.
