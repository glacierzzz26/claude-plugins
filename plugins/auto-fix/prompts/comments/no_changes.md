**auto-fix made no changes.** The fixer finished without modifying any file, so
there is nothing to review and no PR was opened.

This usually means one of:

- the issue is already fixed on the base branch;
- the issue is not actionable as written (missing reproduction, unclear scope);
- the fixer could not locate the relevant code.

If the issue is still valid, add detail (a failing test, file paths, expected
behavior) and re-apply the `auto-fix` label.
